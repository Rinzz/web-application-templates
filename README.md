# AspNet Core WebApi Template

A ready-to-use template for ASP.NET Core WebApi services, models mapping, JWT token authentication, DI.

# Template Renamer

Simple console application can be used to renaming the template.
